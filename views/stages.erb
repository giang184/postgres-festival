<h1>Woodland Mist Festival!</h1>

<h3>WHO P-P-PLAYING? Check out our stages</h3>

<% if @stages.any? %> <%# Array#any? checks if the array contains anything%> 
  <ul>
  <% @stages.each do |stage| %>
    <li><a href="/stages/<%= stage.id %>"><%= stage.name %></a></li>
  <% end %>
  </ul>
<% else %>
  <p>There is currently no music happening. *womp womp*</p>
<% end %>

<a href="/stages/new">Add a new stage?</a>